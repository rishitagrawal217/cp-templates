#include <bits/stdc++.h>
using namespace std;
#define int long long
#define vi vector<int>
#define rep(i,a,b) for(int i=a;i<b;i++) 
#define repr(i,a,b) for(int i=a;i>=b;i--)
#define takeinp1based(v, n) int n; cin>>n;vector<int> v(n+1);for(int i = 1; i <= (n); i++) cin >> v[i];
#define RishitAgrawal ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define takeinp(v, n) int n; cin>>n;vector<int> v(n);for(int i = 0; i < (n); i++) cin >> v[i];
#define vvi vector<vector<int>>
#define vpii vector<pair<int,int>>
#define pb push_back
#define pyes cout<<"YES"<<endl;
#define pno cout<<"NO"<<endl;
const int mod = 1e9+7;   
int modpow(int a, int b) {
    int res = 1;
    while (b > 0) {
        if (b & 1) res = (res * a) % mod;
        a = (a * a) % mod;
        b >>= 1;
    }
    return res;
}
int modinv(int a) {
    return modpow(a, mod - 2);
}
int nCr(int n, int r) {
    if (r < 0 || r > n) return 0;
    if (r > n - r) r = n - r; 
    int res = 1;
    for (int i = 0; i < r; i++) {
        res = (res * (n - i)) % mod;
        res = (res * modinv(i + 1)) % mod; 
    }
    return res;
}
void solve(){
}
signed main() {
    RishitAgrawal;
    int t; cin >> t;
    while(t--) solve();
    return 0;
}
